pytest_test:
  image: python:3.12
  stage: test
  script:
    - curl -LsSf https://astral.sh/uv/install.sh | sh
    - source $HOME/.cargo/env
    - uv sync
    - uv run pytest --cov -v --junitxml=test-results.xml
  only:
    - ci_test