bump_version_dry:
  stage: version
  image: python:3.12-alpine
  script:
    - apk add --no-cache git
    - wget -qO- https://astral.sh/uv/install.sh | sh
    - source $HOME/.cargo/env
    - uv version --bump ${VERSION_TYPE:-patch} --dry-run
  only:
    - ci_test
  when: manual